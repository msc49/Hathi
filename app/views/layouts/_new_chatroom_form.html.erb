
<%= form_with(model: @chatroom, remote: true,format: :turbo_stream, class:'d-flex') do |f| %>
  <%= f.text_field :name, autocomplete: 'off',class: 'form-control', placeholder: 'ʕ•ᴥ•ʔ' %>
  <%= f.submit "Add Room", data: {disable_with: false}, class: 'btn btn-primary' %>
<% end %>